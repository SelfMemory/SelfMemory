# SelfMemory MCP Server Configuration
# Copy this file to .env and update the values for your environment

# ====================================
# Core Configuration
# ====================================

# SelfMemory API Configuration
# This is where your core SelfMemory server is running
# The MCP server will connect to this for authentication and memory operations
SELFMEMORY_API_HOST=http://localhost:8081

# MCP Server Configuration
# Host and port where the MCP server will listen for client connections
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8080

# ====================================
# Optional Configuration
# ====================================

# Logging Configuration
# Set log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Development/Production Mode
# Set to true for development mode with additional logging
DEBUG=false

# ====================================
# Client Usage (for reference only)
# ====================================
# When connecting MCP clients to this server, clients will need:
# 1. Server URL: http://localhost:8080/mcp
# 2. Bearer Token: A valid SelfMemory API key (sk_im_...)
#
# Example client configuration:
# URL: http://localhost:8080/mcp
# Authorization: Bearer sk_im_your_api_key_here
#
# Note: The MCP server does NOT use INMEM_API_KEY directly - 
# it validates tokens sent by clients against the core server

# ====================================
# Advanced Configuration (optional)
# ====================================

# OAuth 2.1 Resource Server Configuration
# These are automatically configured based on SELFMEMORY_API_HOST
# Only override if you need custom authentication settings
# AUTH_ISSUER_URL=http://localhost:8081
# AUTH_RESOURCE_SERVER_URL=http://localhost:8080

# Custom timeout settings (in seconds)
# CLIENT_TIMEOUT=30
# AUTH_TIMEOUT=10
