# Migration Environment Configuration
# Copy this file and fill in your actual database URLs

# ============================================================================
# MONGODB ATLAS CONFIGURATION (REQUIRED)
# ============================================================================
# Replace with your MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://inmemory:5oln59Gt0frZWheN@cluster0.ufznfyx.mongodb.net/inmemory?retryWrites=true&w=majority&appName=Cluster0

# MongoDB connection settings
MONGODB_TIMEOUT=30
MONGODB_MAX_POOL_SIZE=100
MONGODB_RETRY_WRITES=true
MONGODB_WRITE_CONCERN=majority

# ============================================================================
# QDRANT CONFIGURATION (REQUIRED FOR MIGRATION)
# ============================================================================
# For LOCAL Qdrant (if running locally):
# QDRANT_PATH=./qdrant_data

# For QDRANT CLOUD (if using cloud):
QDRANT_URL=http://10.132.3.11:6333
QDRANT_API_KEY=your-api-key-here

# Collection name (must match your existing collection)
QDRANT_COLLECTION_NAME=selfmemory_memories

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================
# Log level for migration
LOG_LEVEL=INFO

# Environment
ENVIRONMENT=migration
