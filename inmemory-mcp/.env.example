# InMemory MCP Server Configuration

# Required: Your InMemory API key (get this from your dashboard)
INMEM_API_KEY=im_your_api_key_here

# Optional: Custom API host (auto-discovered if not set)
# INMEMORY_API_HOST=https://your-inmemory-instance.com

# Optional: MCP server configuration
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
